# This file was generated by Ansible for {{ansible_fqdn}}
# Do NOT modify this file by hand!

upstream {{ app_name }}_server {
    server localhost:{{ app_port }} fail_timeout=0;
}

server {
    listen 80 default_server;


    root {{ app_path }}/collected_static;
    index index.html index.htm;

    access_log /var/log/nginx/{{ app_name }}-access.log timed_combined;
    error_log /var/log/nginx/{{ app_name }}-error.log;

    location /static/ {
        alias {{ app_path }}/collected_static/;
    }

    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $http_host;
      proxy_redirect off;

      if (!-f $request_filename) {
        proxy_pass http://{{ app_name}}_server;
        break;
      }
    }
}
